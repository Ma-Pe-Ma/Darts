<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/playerScoreListTitle"
        android:layout_marginTop="12dp"
        android:layout_marginStart="12dp"
        android:textSize="32sp"
        android:text="@string/dialogSelectorTitle"
        android:textColor="#000000"
        app:layout_constraintTop_toTopOf="parent"/>

    <include android:id="@+id/playerScoreList"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        app:layout_constraintTop_toBottomOf="@id/playerScoreListTitle"
        app:layout_constraintLeft_toLeftOf="parent"
        layout="@layout/player_list_with_header" />

</androidx.constraintlayout.widget.ConstraintLayout>